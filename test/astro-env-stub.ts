/**
 * Stands in for `astro:env/server`, which only exists inside an Astro build.
 * Tests cover pure logic, not runtime configuration.
 */
export const AUTH_SECRET = "test-secret";
export const EDITORS = "";
export const GITHUB_TOKEN = "test-token";
export const GITHUB_REPO = "owner/repo";
export const GITHUB_BRANCH = "main";
