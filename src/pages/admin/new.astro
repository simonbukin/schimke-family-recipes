---
import { getCollection } from "astro:content";
import Layout from "../../layouts/Layout.astro";
import RecipeForm from "../../components/RecipeForm.svelte";

const recipeOptions = (await getCollection("recipes"))
  .map((recipe) => ({ slug: recipe.id, name: recipe.data.name }))
  .sort((a, b) => a.name.localeCompare(b.name));
---

<Layout title="Add a recipe">
  <main class="min-h-screen bg-page p-4 max-w-2xl mx-auto">
    <a href="/admin" class="inline-flex items-center min-h-[44px] text-accent hover:text-accent-strong">
      ← All recipes
    </a>
    <h1 class="text-2xl font-bold text-ink mb-4">Add a recipe</h1>
    <RecipeForm client:load recipeOptions={recipeOptions} />
  </main>
</Layout>
